/**
 * Point d'entrée unique pour tous les composants UI.
 * Permet d'importer depuis "@/components/ui" au lieu de chemins individuels.
 *
 * Usage : import { Button, Input, Card } from "@/components/ui"
 */

export { Button } from "./button";
export { Input } from "./input";
export { Textarea } from "./textarea";
export { Select } from "./select";
export { Badge, StatusBadge, STATUS_LABELS } from "./badge";
export { Dialog, DialogContent } from "./dialog";
export { Card, CardHeader, CardTitle, CardDescription, CardBody, CardFooter } from "./card";
